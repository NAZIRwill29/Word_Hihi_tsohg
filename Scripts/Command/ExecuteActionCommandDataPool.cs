using System.Collections.Generic;

//LEFTPROJ ()
public static class ExecuteActionCommandDataPool
{
    // private static readonly Queue<ExecuteActionCommandData> _pool = new();

    // public static ExecuteActionCommandData GetData()
    // {
    //     if (_pool.Count > 0)
    //     {
    //         var data = _pool.Dequeue();
    //         data.Reset();  // Ensure it's cleaned up before reuse
    //         return data;
    //     }
    //     return new ExecuteActionCommandData();  // Create a new one if empty
    // }

    // public static void ReturnData(ExecuteActionCommandData data)
    // {
    //     data.Reset();  // Reset before putting it back
    //     _pool.Enqueue(data);
    // }
}
